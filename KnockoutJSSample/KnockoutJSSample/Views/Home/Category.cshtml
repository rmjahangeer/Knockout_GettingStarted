<h2>@ViewBag.Title</h2>
<small><a href="@Url.Action("AddCategory")">Add new</a></small>
@section scripts
{
    <script>
        window.view = 'Index';
    </script>
    <script src="~/App_Scripts/category.js" type="text/javascript"></script>
}

<div class="well well-lg">
    <table class="table table-hover table-responsive">
        <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
        </tr>
        </thead>
        <tbody data-bind="foreach : categories">
        <tr>
            <td><a data-bind="attr : { href : '@Url.Action("AddCategory")/' + Id, title : 'Edit ' + Name }, text : Id"></a></td>
            <td data-bind="text : Name"></td>
            <td>
                <button class="btn btn-group-sm btn-danger" data-bind="click : remove">
                    <i class="glyphicon glyphicon-trash"></i>
                </button>
            </td>
        </tr>
        </tbody>
    </table>
</div>




@{
    ViewBag.Title = "Category";
}

<h2>Category</h2>

